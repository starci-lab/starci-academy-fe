import { cn } from "@heroui/react"

/** Pair the result identity with its decisive score without localizing the shell anatomy. */
export const mockInterviewResultHeroClassName = `${cn("grid", "min-w-0", "overflow-hidden", "rounded-3xl", "bg-accent-soft", "shadow-[var(--starci-core-surface-shadow)]")} lg:grid-cols-[minmax(0,1fr)_18rem]`
/** Keep result context readable before the score panel. */
export const mockInterviewResultHeroCopyClassName = cn("flex", "min-w-0", "flex-col", "items-start", "gap-4", "p-5", "sm:p-6", "lg:p-8")
/** Center the result score as the hero's decisive fact. */
export const mockInterviewResultHeroScoreClassName = cn("flex", "min-w-0", "flex-col", "items-center", "justify-center", "gap-2", "bg-accent/10", "p-6", "text-center")
/** Give the overall score the strongest numeric hierarchy. */
export const mockInterviewResultScoreClassName = cn("text-4xl", "font-bold", "tracking-tight", "text-accent-soft-foreground", "sm:text-5xl")
/** Stack the evidence sections with terminal breathing room. */
export const mockInterviewResultPrimaryClassName = cn("flex", "min-w-0", "flex-col", "gap-6", "pb-12", "max-sm:pr-20", "sm:pb-16")
/** Stack session context and next actions in the supporting rail. */
export const mockInterviewResultRailClassName = cn("flex", "min-w-0", "flex-col", "gap-6", "max-sm:pr-20")
/** Arrange repeated evidence rows inside one owned surface. */
export const mockInterviewResultListClassName = cn("flex", "min-w-0", "flex-col", "gap-4", "p-5")
/** Preserve authored review sections so question, answer and feedback remain scannable. */
export const mockInterviewResultReviewsClassName = cn("whitespace-pre-line")
/** Place strengths and gaps as peer evidence owners. */
export const mockInterviewResultEvidenceClassName = cn("grid", "min-w-0", "grid-cols-1", "gap-6", "md:grid-cols-2")
/** Keep compact session facts vertically scannable. */
export const mockInterviewResultSummaryClassName = cn("flex", "min-w-0", "flex-col", "gap-4", "p-5")
/** Wrap result actions without weakening their ordered priority. */
export const mockInterviewResultActionsClassName = cn("flex", "min-w-0", "flex-wrap", "gap-2")
/** Stack every result destination as one consistent compact action set and restore wrapping on wider surfaces. */
export const mockInterviewResultReadyActionsClassName = cn("grid", "min-w-0", "grid-cols-1", "gap-2", "[&>button]:w-full", "sm:flex", "sm:flex-wrap", "sm:[&>button]:w-auto")
/** Keep transitional result states attached to the hero so progress reads as the next step, not a second screen. */
export const mockInterviewResultStateClassName = cn("mx-auto", "flex", "w-full", "max-w-3xl", "min-w-0", "flex-col", "pb-12", "lg:min-h-[calc(100dvh-20rem)]", "lg:justify-center")
/** Bound the transcript dialog while preserving a visible heading. */
export const mockInterviewResultTranscriptClassName = cn("flex", "h-[calc(100dvh-6rem)]", "max-h-[calc(100dvh-6rem)]", "min-w-0", "flex-col", "gap-5", "overflow-hidden", "rounded-2xl", "bg-background", "p-5", "sm:p-6")
/** Own transcript scrolling inside the dialog rather than the page behind it. */
export const mockInterviewResultTranscriptListClassName = cn("flex", "min-h-0", "min-w-0", "flex-col", "gap-4", "overflow-y-auto", "overscroll-contain", "pe-1")
/** Keep one question and answer paired as a single transcript turn group. */
export const mockInterviewResultTranscriptItemClassName = cn("flex", "min-w-0", "flex-col", "gap-3", "p-5")
