import { ModalBranch } from "@/components/branches/ModalBranch"
import { SurfaceCard } from "@/components/branches/SurfaceCard"
import { SurfaceListCard } from "@/components/branches/SurfaceListCard"
import { EmptyNotice } from "@/components/composites/EmptyNotice"
import { Button } from "@/components/leaves/Button"
import { ChoiceTabs } from "@/components/leaves/ChoiceTabs"
import { Heading } from "@/components/leaves/Heading"
import { SearchBox } from "@/components/leaves/SearchBox"
import { Text } from "@/components/leaves/Text"
/** One settled deck row. */
export type FlashcardReviewDeckRow = { readonly id: string; readonly title: string; readonly description: string; readonly difficulty: string; readonly cardCount: number; readonly dueCount: number; readonly masteredCount: number; readonly quizEligible: boolean }
/** One comparable history or review-health row. */
export type FlashcardReviewEvidenceRow = { readonly id: string; readonly title: string; readonly description: string; readonly fact: string }
/** Evidence panel tab. */
export type FlashcardReviewView = "overview" | "history" | "stats"
/** Deck presentation layout. */
export type FlashcardReviewLayout = "grid" | "line"
/** Transport state for review workspace. */
export type CourseFlashcardsReviewBlockState = "pending" | "ready" | "empty" | "failed"
type ReviewScope = "all" | "due"
/** Pure review page data and actions. */
export type CourseFlashcardsReviewProps = { readonly pageState: FlashcardReviewView; readonly blockState: CourseFlashcardsReviewBlockState; readonly props: { readonly title: string; readonly subtitle: string; readonly reviewLabel: string; readonly quizLabel: string; readonly modeTabsLabel: string; readonly viewTabsLabel: string; readonly overviewLabel: string; readonly historyLabel: string; readonly statsLabel: string; readonly activeView: FlashcardReviewView; readonly dueTitle: string; readonly dueDescription: string; readonly decksTitle: string; readonly evidenceTitle: string; readonly cardsLabel: string; readonly dueLabel: string; readonly masteredLabel: string; readonly startLabel: string; readonly quizDeckLabel: string; readonly resumeLabel: string; readonly retryLabel: string; readonly emptyText: string; readonly evidenceEmptyText: string; readonly noResultsText: string; readonly failedText: string; readonly dueCount: number; readonly statRows: ReadonlyArray<{ readonly label: string; readonly value: string }>; readonly decks: ReadonlyArray<FlashcardReviewDeckRow>; readonly evidenceRows: ReadonlyArray<FlashcardReviewEvidenceRow>; readonly searchLabel: string; readonly searchClearLabel: string; readonly searchValue: string; readonly foundText: string; readonly layoutLabel: string; readonly gridLabel: string; readonly lineLabel: string; readonly layout: FlashcardReviewLayout; readonly resumeSessionId?: string; readonly modalOpen: boolean; readonly modalTitle: string; readonly modalDescription: string; readonly reviewAllLabel: string; readonly reviewDueLabel: string; readonly cancelLabel: string; readonly selectedScope: ReviewScope; readonly selectedDeckId?: string; readonly startPending: boolean; readonly startErrorText?: string }; readonly on: { readonly openQuiz: (deckId?: string) => void; readonly selectView: (view: FlashcardReviewView) => void; readonly changeSearch: (value: string) => void; readonly changeLayout: (layout: FlashcardReviewLayout) => void; readonly openReview: (deckId: string) => void; readonly startDue: () => void; readonly selectScope: (scope: ReviewScope) => void; readonly confirmReview: () => void; readonly dismissModal: () => void; readonly resume: (sessionId: string) => void; readonly retry: () => void } }
/** Public props for the review block presentation. */
export type CourseFlashcardsReviewBlockProps = CourseFlashcardsReviewProps
/** Compatibility export for existing imports. */

/** Draw review overview, evidence panels and review-scope modal without fetching. */
export const CourseFlashcardsReviewBlockBase = (props: CourseFlashcardsReviewBlockProps) => {
    const data = props.props
    const loading = props.blockState === "pending"
    if (props.blockState === "failed" || props.blockState === "empty") return <EmptyNotice props={{ message: props.blockState === "failed" ? data.failedText : data.emptyText, actionLabel: props.blockState === "failed" ? data.retryLabel : undefined }} on={{ act: props.on.retry }} />
    return <><main aria-label={data.title}><header><Heading props={{ content: data.title, level: 1 }} /><Text props={{ content: data.subtitle, size: "sm", tone: "muted" }} /></header><ChoiceTabs props={{ label: data.modeTabsLabel, selectedKey: "review", variant: "primary", tabs: [{ id: "review", label: data.reviewLabel }, { id: "quiz", label: data.quizLabel }] }} on={{ select: (key) => { if (key === "quiz") props.on.openQuiz() } }} /><ChoiceTabs props={{ label: data.viewTabsLabel, selectedKey: data.activeView, variant: "secondary", tabs: [{ id: "overview", label: data.overviewLabel }, { id: "history", label: data.historyLabel }, { id: "stats", label: data.statsLabel }] }} on={{ select: (key) => { if (key === "overview" || key === "history" || key === "stats") props.on.selectView(key) } }} />{props.pageState === "overview" ? <><SurfaceCard props={{ label: data.dueTitle }}><Text props={{ content: data.dueDescription, size: "sm", tone: "muted" }} /><Text props={{ content: `${data.dueCount} ${data.dueLabel}`, size: "sm", weight: "medium" }} />{data.resumeSessionId === undefined ? <Button props={{ label: data.startLabel, variant: "primary", isPending: data.startPending }} on={{ press: props.on.startDue }} /> : <Button props={{ label: data.resumeLabel, variant: "primary" }} on={{ press: () => props.on.resume(data.resumeSessionId!) }} />}</SurfaceCard><section aria-label={data.decksTitle}><SearchBox props={{ placeholder: data.searchLabel, label: data.searchLabel, clearLabel: data.searchClearLabel }} on={{ search: props.on.changeSearch }} /><Text props={{ content: data.foundText, size: "sm", tone: "muted" }} isLoading={loading} /><div>{data.decks.map((deck) => <SurfaceCard key={deck.id}><Heading props={{ content: deck.title, level: 3 }} /><Text props={{ content: deck.description, size: "sm", tone: "muted" }} /><Text props={{ content: [deck.difficulty, `${deck.cardCount} ${data.cardsLabel}`, `${deck.dueCount} ${data.dueLabel}`, `${deck.masteredCount} ${data.masteredLabel}`].join(" · "), size: "xs", tone: "muted" }} /><Button props={{ label: data.startLabel, size: "sm", variant: "outline" }} on={{ press: () => props.on.openReview(deck.id) }} />{deck.quizEligible ? <Button props={{ label: data.quizDeckLabel, size: "sm", variant: "outline" }} on={{ press: () => props.on.openQuiz(deck.id) }} /> : null}</SurfaceCard>)}</div></section></> : <SurfaceListCard props={{ label: data.evidenceTitle }}><ul>{data.evidenceRows.map((row) => <li key={row.id}><Text props={{ content: row.title, size: "sm", weight: "medium" }} /><Text props={{ content: row.description, size: "sm", tone: "muted" }} /><Text props={{ content: row.fact, size: "xs", tone: "muted" }} /></li>)}</ul></SurfaceListCard>}</main><ModalBranch isOpen={data.modalOpen} size="md" onDismiss={props.on.dismissModal}><Heading props={{ content: data.modalTitle, level: 2 }} /><Text props={{ content: data.modalDescription, size: "sm", tone: "muted" }} /><Button props={{ label: data.reviewAllLabel, variant: data.selectedScope === "all" ? "primary" : "outline" }} on={{ press: () => props.on.selectScope("all") }} /><Button props={{ label: data.reviewDueLabel, variant: data.selectedScope === "due" ? "primary" : "outline" }} on={{ press: () => props.on.selectScope("due") }} /><Button props={{ label: data.cancelLabel, variant: "outline" }} on={{ press: props.on.dismissModal }} /><Button props={{ label: data.startLabel, variant: "primary", isPending: data.startPending }} on={{ press: props.on.confirmReview }} /></ModalBranch></>
}
