import { cn } from "@heroui/react"

/** Stack all setup destinations below the route hero. */
export const mockInterviewWorkspaceClassName = cn("flex", "w-full", "min-w-0", "flex-col", "gap-6", "pb-12", "sm:pb-16")
/** Orient the learner with feature identity, interview media, and session limits. */
export const mockInterviewHeroClassName = `${cn("grid", "min-w-0", "overflow-hidden", "rounded-3xl", "bg-accent-soft", "shadow-[var(--starci-core-surface-shadow)]")} lg:grid-cols-[minmax(0,1fr)_22rem_18rem]`
/** Keep setup identity and its preparation action in one reading group. */
export const mockInterviewHeroCopyClassName = cn("flex", "min-w-0", "flex-col", "items-start", "gap-4", "p-5", "sm:p-6", "lg:p-8")
/** Keep the preparation action attached to the hero copy. */
export const mockInterviewHeroActionClassName = cn("flex", "min-w-0", "flex-wrap", "items-center", "gap-2")
/** Purpose-built feature orientation media; hidden only when the compact stack cannot give it useful space. */
export const mockInterviewHeroMediaClassName = cn("relative", "hidden", "min-h-52", "items-center", "justify-center", "overflow-hidden", "bg-accent/5", "sm:flex", "lg:min-h-0")
/** Preserve the transparent interview scene without cropping people or the microphone. */
export const mockInterviewHeroImageClassName = cn("h-full", "max-h-64", "w-full", "object-contain", "object-center", "p-3", "lg:max-h-none", "lg:p-2")
/** Present immutable interview limits as a subordinate fact strip. */
export const mockInterviewHeroFactsClassName = cn("grid", "m-0", "min-w-0", "grid-cols-3", "bg-accent/10", "p-4", "sm:p-5", "lg:grid-cols-1", "lg:content-center", "lg:p-6")
/** Bind one fact label to its value across strip and rail arrangements. */
export const mockInterviewHeroFactClassName = cn("flex", "min-w-0", "flex-col", "gap-1", "border-accent/20", "px-3", "text-center", "[&+&]:border-l", "lg:px-0", "lg:py-3", "lg:text-left", "lg:[&+&]:border-l-0", "lg:[&+&]:border-t")
/** Expose overview, history, and statistics as sibling destinations. */
export const mockInterviewDestinationNavClassName = cn("min-w-0", "overflow-x-auto", "border-b", "border-separator", "pb-1")
/** Group session choices into one setup form surface. */
export const mockInterviewSetupFormClassName = cn("flex", "min-w-0", "flex-col", "gap-5", "p-5", "sm:p-6", "focus:outline-none")
/** Pair setup decisions with their launch evidence without a detached sticky rail. */
export const mockInterviewLaunchClassName = `${cn("grid", "min-w-0", "grid-cols-1", "items-start", "gap-6")} lg:grid-cols-[minmax(0,1fr)_20rem]`
/** Bind a choice label, control, and explanation. */
export const mockInterviewFieldClassName = cn("flex", "min-w-0", "flex-col", "gap-2")
/** Keep the launch summary and action in one bounded surface. */
export const mockInterviewPreflightClassName = cn("flex", "min-w-0", "flex-col", "gap-5", "p-5", "sm:p-6")
/** Stack the readiness evidence shown before launch. */
export const mockInterviewSummaryClassName = cn("flex", "min-w-0", "flex-col", "gap-3")
/** Bind one readiness label to its current value. */
export const mockInterviewFactClassName = cn("flex", "min-w-0", "flex-col", "gap-1")
/** Pair recent attempts with practice progress after first use. */
export const mockInterviewDashboardClassName = cn("grid", "min-w-0", "grid-cols-1", "items-start", "gap-6", "lg:grid-cols-2")
/** Preserve the history list's independent width inside the dashboard. */
export const mockInterviewHistoryPanelClassName = cn("min-w-0")
/** Remove browser list chrome from evidence rows. */
export const mockInterviewEvidenceListClassName = cn("m-0", "list-none", "p-0", "[&>*+*]:border-t", "[&>*+*]:border-separator")
/** Align one attempt summary with its result action. */
export const mockInterviewEvidenceRowClassName = cn("flex", "min-w-0", "items-center", "gap-3", "px-4", "py-3")
/** Stack one attempt's title and timestamp. */
export const mockInterviewEvidenceCopyClassName = cn("flex", "min-w-0", "flex-1", "flex-col", "gap-1", "break-words")
/** Stack statistics rows and the destination action. */
export const mockInterviewProgressClassName = cn("flex", "min-w-0", "flex-col", "gap-4", "p-5")
/** Center a concise empty or loading notice inside a dashboard surface. */
export const mockInterviewCompactStateClassName = cn("flex", "min-h-40", "min-w-0", "flex-col", "justify-center", "p-5")
/** Give terminal setup states a readable minimum surface. */
export const mockInterviewStateClassName = cn("flex", "min-h-64", "min-w-0", "max-w-2xl", "flex-col", "items-start", "justify-center", "gap-4", "p-6", "sm:p-8")
/** Pair resumable-session context with its continuation action. */
export const mockInterviewResumeClassName = cn("flex", "min-w-0", "flex-col", "items-start", "gap-5", "p-5", "sm:flex-row", "sm:items-center", "sm:justify-between", "sm:p-6")
/** Stack resumable-session identity and status. */
export const mockInterviewResumeCopyClassName = cn("flex", "min-w-0", "flex-1", "flex-col", "gap-2")
/** Group subordinate setup actions without forcing one row. */
export const mockInterviewActionRowClassName = cn("flex", "min-w-0", "flex-wrap", "gap-2")
