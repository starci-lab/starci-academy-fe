import { render, screen } from "@testing-library/react"; import { describe, expect, it } from "vitest"; import { ProblemReadingColumnBase } from "./component"
describe("ProblemReadingColumn",()=>{it("renders the problem reading content",()=>{render(<ProblemReadingColumnBase state="ready" props={{tab:"statement",tabLabels:{statement:"Statement",hint:"Hint",solution:"Solution",submissions:"Submissions",group:"Views"},title:"Two sum",difficulty:"Easy",body:"Find a pair",tags:["arrays"]}}/>);expect(screen.getByRole("heading",{name:"Two sum"})).toBeInTheDocument();expect(screen.getByText("Find a pair")).toBeInTheDocument();expect(screen.getByText("arrays")).toBeInTheDocument()})})
