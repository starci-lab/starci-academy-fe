import { cn } from "@heroui/react"

/** Keep each activity day in the feed's vertical reading order. */
export const activityFeedClassName = cn("flex", "min-w-0", "flex-col", "gap-6")

/** Bind one calendar label to its activity rows. */
export const activityDayClassName = cn("flex", "min-w-0", "flex-col", "gap-2")
