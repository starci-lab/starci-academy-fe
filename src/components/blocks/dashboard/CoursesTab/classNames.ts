import { cn } from "@heroui/react"

/** Give each independently settling Courses block a stable, scannable rhythm. */
export const coursesTabClassName = cn("flex", "min-w-0", "flex-col", "gap-6")

/** Full-width enrolled progress anchor for the Courses tab. */
export const coursesLeadClassName = cn("min-w-0")

/** One peer column carrying recommendation decisions and pricing context. */
export const coursesRecommendationClassName = cn("min-w-0")
