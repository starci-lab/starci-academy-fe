import { cn } from "@heroui/react"
import { dashboardRailRowItemClassName } from "@/components/blocks/dashboard/classNames"

/** Place the streak fact on the shared identity-rail row rhythm. */
export const streakStatRowClassName = cn(dashboardRailRowItemClassName)
