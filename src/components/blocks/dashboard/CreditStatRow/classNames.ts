import { cn } from "@heroui/react"
import { dashboardRailRowItemClassName } from "@/components/blocks/dashboard/classNames"

/** Place the credit fact on the shared identity-rail row rhythm. */
export const creditStatRowClassName = cn(dashboardRailRowItemClassName)
