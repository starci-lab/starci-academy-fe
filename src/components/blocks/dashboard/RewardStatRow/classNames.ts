import { cn } from "@heroui/react"
import { dashboardRailRowItemClassName } from "@/components/blocks/dashboard/classNames"

/** Place the reward fact on the shared identity-rail row rhythm. */
export const rewardStatRowClassName = cn(dashboardRailRowItemClassName)
