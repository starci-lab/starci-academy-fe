import { cn } from "@heroui/react"
import { formFieldClassName as grammarFormFieldClassName } from "@starci/grammar/core"

/** Grammar-owned label-control-help field anatomy. */
export const fieldClassName = cn(grammarFormFieldClassName)
