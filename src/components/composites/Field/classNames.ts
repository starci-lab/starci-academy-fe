import { cn } from "@heroui/react"
import { formFieldClassName as grammarFormFieldClassName } from "@starci/grammar/common"

/** Grammar-owned label-control-help field anatomy; this app alias adds no visual override. */
export const fieldClassName = cn(grammarFormFieldClassName)
