import { cn } from "@heroui/react"
/** Outer form surface grouping. */
export const formSurfaceClassName = cn("flex", "flex-col", "gap-3")
/** Bounded form surface frame. */
export const formSurfaceFrameClassName = cn("w-full", "p-0")
/** Form surface content without vendor inset. */
export const formSurfaceContentClassName = cn("p-0")
/** Optional form heading row. */
export const formSurfaceLabelClassName = cn("flex", "items-center", "gap-3")
