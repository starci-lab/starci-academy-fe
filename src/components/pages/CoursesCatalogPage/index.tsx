"use client"
import { CoursesCatalogPageBase } from "./component"
/** Connected catalog route entry. */
export const CoursesCatalogPage = () => <CoursesCatalogPageBase />
/** Ownership metadata for the route entry. */
export const meta = { world: "connected", domain: "courses" } as const
