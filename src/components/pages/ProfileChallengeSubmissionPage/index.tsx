"use client"

import { ProfileChallengeSubmissionPageBase } from "./component"

/** Render the route shell; params and evidence state belong to the connected block. */
export type ProfileChallengeSubmissionPageProps = Record<never, never>
/** Render the connected profile challenge submission route. */
export const ProfileChallengeSubmissionPage = (props: ProfileChallengeSubmissionPageProps) => { void props; return <ProfileChallengeSubmissionPageBase {...{}} /> }

export * from "./component"
