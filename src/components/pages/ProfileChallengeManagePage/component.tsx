import { ProfileChallengeManage } from "@/components/blocks/profile/ProfileChallengeManage"

/** Profile route shell; the connected challenge-management block owns profile-main content. */
export type ProfileChallengeManagePageProps = Record<never, never>
/** Render the profile challenge management block. */
export const ProfileChallengeManagePageBase = (props: ProfileChallengeManagePageProps) => { void props; return <ProfileChallengeManage /> }
