"use client"

import { ProfileChallengeManagePageBase } from "./component"

/** Render the route shell; route params and evidence state belong to the connected block. */
export type ProfileChallengeManagePageProps = Record<never, never>
/** Render the connected profile challenge management route. */
export const ProfileChallengeManagePage = (props: ProfileChallengeManagePageProps) => { void props; return <ProfileChallengeManagePageBase {...{}} /> }

export * from "./component"
