import { ProfileCodingProblem } from "@/components/blocks/profile/ProfileCodingProblem"

/** Profile route shell; the connected coding-proof block owns profile-main content. */
export type ProfileDetailPageProps = Record<never, never>
/** Render the profile coding detail block. */
export const ProfileDetailPageBase = (props: ProfileDetailPageProps) => { void props; return <ProfileCodingProblem /> }
