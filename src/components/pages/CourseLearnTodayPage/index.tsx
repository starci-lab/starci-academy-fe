"use client"

import { CourseLearnTodayPageBase, type CourseLearnTodayPageProps } from "./component"

/** Connected route entry that supplies route identity only. */
export const CourseLearnTodayPage = (props: CourseLearnTodayPageProps) => <CourseLearnTodayPageBase {...props} />

/** Ownership metadata for the route entry. */
