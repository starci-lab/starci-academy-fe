import { ProfileProjectRoadmap } from "@/components/blocks/profile/ProfileProjectRoadmap"

/** Profile route shell; the connected roadmap block owns profile-main content. */
export type ProfileProjectRoadmapPageProps = Record<never, never>
/** Render the profile roadmap block. */
export const ProfileProjectRoadmapPageBase = (props: ProfileProjectRoadmapPageProps) => { void props; return <ProfileProjectRoadmap /> }
