"use client"
import { ProfileChallengesPageBase } from "./component"
/** Render the profile challenge page shell. */
export type ProfileChallengesPageProps = Record<never, never>
/** Render the connected profile challenges route. */
export const ProfileChallengesPage = (props: ProfileChallengesPageProps) => { void props; return <ProfileChallengesPageBase {...{}} /> }
export * from "./component"
