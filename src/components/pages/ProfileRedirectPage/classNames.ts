import { cn } from "@heroui/react"

/** Bounded Profile entry state below the persistent application header. */
export const profileRedirectStateClassName = cn("mx-auto", "w-full", "max-w-app-xl", "p-6")
