"use client"
import { ProfileActivityPageBase } from "./component"
/** Render the activity page shell. */
export type ProfileActivityPageProps = Record<never, never>
/** Render the connected profile activity route. */
export const ProfileActivityPage = (props: ProfileActivityPageProps) => { void props; return <ProfileActivityPageBase {...{}} /> }
export * from "./component"
