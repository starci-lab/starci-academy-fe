import { ProfileProjects } from "@/components/blocks/profile/ProfileProjects"
/** Projects page shell; the connected block owns the profile-main anatomy. */
export type ProfileProjectsPageProps = Record<never, never>
/** Render the profile projects block. */
export const ProfileProjectsPageBase = (props: ProfileProjectsPageProps) => { void props; return <ProfileProjects /> }
