"use client"

import { CourseLearnModulePageBase, type CourseLearnModulePageProps } from "./component"

/** Connected route entry that supplies route identity only. */
export const CourseLearnModulePage = (props: CourseLearnModulePageProps) => <CourseLearnModulePageBase {...props} />

/** Ownership metadata for the route entry. */
