import { cn } from "@heroui/react"

/** Keep the feature-owned settings surface attached to the complete dynamic viewport. */
export const personalProjectGradingSettingsDrawerViewportClassName = cn("min-h-[calc(100dvh-4rem)]", "min-w-0")
