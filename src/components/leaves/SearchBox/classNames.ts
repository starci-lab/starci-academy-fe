import { cn } from "@heroui/react"

/** Search field width. */
export const searchBoxClassName = cn("w-full", "max-w-xs")
/** Clear button affordance. */
export const searchBoxClearClassName = cn("shrink-0", "cursor-pointer", "text-muted", "opacity-60", "transition-opacity", "hover:opacity-100", "focus-visible:opacity-100")
/** Keyboard hint appearance. */
export const searchBoxShortcutClassName = cn("shrink-0", "rounded", "border", "px-2", "py-1", "text-xs", "text-muted")
