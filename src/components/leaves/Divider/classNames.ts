import { cn } from "@heroui/react"

/** Horizontal separator layout around the accessible label. */
export const dividerClassName = cn("flex", "flex-row", "items-center", "gap-3")
/** Flexible rule segments framing the separator label. */
export const dividerRuleClassName = cn("h-px", "grow", "bg-border")
