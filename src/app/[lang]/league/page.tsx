import { LeaguePage } from "@/components/pages/LeaguePage"

/** The full leaderboard, behind the "view leaderboard" link on both dashboard cards. */
const Page = () => <LeaguePage />

export default Page
