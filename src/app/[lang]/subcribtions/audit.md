# Legacy `/subcribtions` redirect audit

> This misspelled route is compatibility-only. Its canonical destination is `/[lang]/subscriptions`.

- Change level: `new`
- Layout ceiling: `surface-only`; the route mounts the existing `ShellNav` without changing an ancestor layout.
- Business authority: one StarCi Pro offer at backend-mounted price, one paid month, full published learner access, metered AI, backend-verified activation, manual repeat-purchase consent.
- Legacy reference adopted: every GraphQL write passes through `useGraphQLWithToast`; queries remain SWR reads.
- Product-family reference adopted: the existing PayOS `CheckoutOverlayBase` and its webhook-owned completion language.
- External references: one-offer education subscription hierarchy, transparent plan disclosures, hosted checkout handoff and verification states.
- Explicit exclusions: tax/refund claims, automatic renewal claims, unlimited AI, legacy-customer migration, parent shell redesign, backend mutation.
- Required states: loading, offer failure/retry, signed out, eligible, checkout-opening failure, returned/payment verification pending, active.
- Focused gates: `npm run typecheck` passed; targeted ESLint passed; 6 focused Vitest assertions passed.
- Runtime evidence before fail-closed tightening: wide populated layout rendered; constrained measurement recorded `innerWidth=426`, `bodyScrollWidth=427`, and both regions at `394.67px`; checkout modal fitted the constrained viewport and exposed close, PayOS, and back actions. The PayOS action was not invoked.
- Blind review verdict: `blocked` by central API generation 3, which does not expose the new `proOffer` schema. Fresh review correctly saw the fail-closed retry state, so populated wide/mobile/modal rasters must be recaptured after the central runtime owner refreshes the API.
- Source repair 2026-09-02: `queryProOffer` now uses the anonymous Apollo transport, so an invalid or stale viewer credential cannot poison the public offer. `queryMyProSubscription` and `purchaseProSubscription` remain authenticated boundaries. A focused transport test locks that separation.
- Fresh source gates after the repair: `npm run typecheck` passed; targeted ESLint passed; 3 focused Vitest files / 8 tests passed.
- Fresh runtime probe after the user reported the root source running: frontend returned HTTP 200, while API still returned GraphQL validation failure `Cannot query field "proOffer" on type "Query"`. The populated happy path remains blocked on the central runtime owner refresh; no listener or PayOS action was mutated by this mission.
- Counterevidence resume 2026-09-02: the earlier constrained-width proof measured page width and modal fit but did not include the fixed assistant collision, the short-height modal scroll endpoint, or the dialog accessible-name/modal attributes. Fresh blind review contradicted the implied responsive-readiness claim at 480×844 and 480×640.
- Repair ownership: the fixed assistant remains an immutable global-widget owner; the Pro page now reserves compact inline-end space instead. Checkout content no longer creates a nested scrolling region; HeroUI's modal body is explicitly the single inside-scroll owner. `ModalBranch` now binds the checkout's visible title through `aria-labelledby` and restores `aria-modal=true` on the rendered dialog node because React Aria filters that prop from `Dialog`.
- Focused repair gates: full typecheck passed; targeted ESLint passed; 5 focused Vitest files / 19 tests passed, including compact safe-area, single-scroll-owner, reachable action composition, accessible dialog naming, modal semantics, Pro state, toast, and GraphQL transport regressions.
- Live mechanical proof after hot reload: `/vi/subcribtions` returned HTTP 200 and its rendered source contains the compact safe-area class; generation 4 API returned `proOffer.success=true`, `priceVnd=229000`, `billingPeriodMonths=1`, `enabled=true`. This child task had no available in-app Browser backend, so fresh raster recapture remains with the parent blind-review boundary. No listener or PayOS action was mutated.
- Fresh-review close-label repair 2026-09-02: the prior semantics repair named the dialog but left HeroUI's icon-only close control on its English fallback. The checkout label contract now carries resolved `close` copy, Pro supplies `Đóng` in Vietnamese and `Close` in English, and `ModalBranch` forwards it as the close trigger's accessible label. Typecheck and targeted ESLint passed; 2 minimal focused Vitest files / 10 tests passed. Cart compatibility is preserved by keeping the new shared label input optional for existing callers.
- Final blind review PASS 2026-09-02: at 480×844 the document remained 480/480 with no horizontal overflow and the fixed AI bubble did not intersect CTA or content. At 480×640 the localized `Đóng` close control, named modal dialog, one body scroll owner (`560/730`, max 170), and both checkout actions were fully reachable at maximum scroll; PayOS was not clicked. At 1440×900 the populated 229,000 VND offer, four benefit groups, disclosures, pricing rail and CTA were coherent with no overflow. Fresh isolated reviewer verdict: `PASS`; add/change/remove were all rejected because no material gap remained.
