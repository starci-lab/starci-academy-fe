{
  "name": "starci-coding-practice-candidate",
  "version": "0.0.0",
  "private": true,
  "description": "Preview candidate for case-coding-practice / direction-path-first. Artifact-only: never shipped, never imported by production.",
  "scripts": {
    "build": "next build",
    "dev": "next dev",
    "typecheck": "tsc --noEmit"
  }
}
