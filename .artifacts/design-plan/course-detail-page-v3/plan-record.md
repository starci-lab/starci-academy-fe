# Plan record - case-course-detail-v3

**Status:** direction-selected · **Selected:** `direction-parity-rail` (A) · **Selection:** explicit

## What this case is, and what it is not

Seven items were requested. **Two were already built**: `course-promise-list` and
`course-module-list` are joined `SurfaceListCard` lists today, and the module list is an `ol`
because modules are ordered. They are excluded rather than dressed up as work.

Five items remained, and they are one case rather than five because they compete for one scarce
thing: the reading order of the narrative column and what the rail is for.

## The selected direction

The rail decides, and it does not move. Prerequisites become a gate under the promises; the discount
ladder stays in the rail; trial and cart join the rail under the primary enrol button; the review
block sits at the foot after the curriculum. Nothing that works today moves.

It also absorbs the older `cart` case, whose direction was selected and never previewed, rather
than leaving two cases competing for one rail.

## What it costs

The reviews sit furthest from the moment of decision — which is the one thing the backend built this
session exists to serve. That is the trade the parity posture makes, and it is recorded rather than
argued away.

## Still open for Preview

- Whether an empty review block at the foot of the page reads as 'no reviews yet' or as a broken region. Direction A places it there and cannot answer this from the registry.
- Whether the trial button should disappear or disable once a trial has been used. The backend has startTrial but the page has no state evidence for a spent trial.
- Whether the rail's discount ladder and the new review summary ever compete for the same fold on a short laptop. Only a rendered state can say.
