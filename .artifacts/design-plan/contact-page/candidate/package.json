{
  "name": "starci-contact-page-candidate",
  "version": "0.0.0",
  "private": true,
  "description": "Preview candidate for case-contact-page / direction-one-door. Artifact-only: never shipped, never imported by production.",
  "scripts": {
    "build": "next build",
    "dev": "next dev",
    "typecheck": "tsc --noEmit"
  }
}
