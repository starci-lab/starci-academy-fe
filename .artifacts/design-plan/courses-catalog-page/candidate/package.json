{
  "name": "starci-courses-catalog-candidate",
  "version": "0.0.0",
  "private": true,
  "description": "Preview candidate for case-courses-catalog / direction-enrollment-split. Artifact-only: never shipped, never imported by production.",
  "scripts": {
    "build": "next build",
    "dev": "next dev",
    "typecheck": "tsc --noEmit"
  }
}
