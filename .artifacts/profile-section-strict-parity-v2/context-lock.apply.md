# Context Lock — Apply awaiting confirmation

Approved case `strict-parity` is frozen. No repository, branch, worktree, HEAD or remote drift was
detected from Preview.

Production writes are limited to the profile routes/components/contracts/profile-evidence query and
EN/VI messages listed in `context-lock.apply.json`, all inside `D:/Repositories/starci-academy-fe`
on branch `main`. Legacy `starci-academy` and backend `starci-academy-backend` remain read-only.
Dependencies, generators, service mutations, new public APIs, vocabulary and backend behavior are
forbidden.

Status remains `awaiting-confirmation` until the user explicitly reconfirms this exact lock.
